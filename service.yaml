apiVersion: v1
kind: Service
metadata:
  name: flask-portfolio-service
  namespace: flask-app
spec:
  type: LoadBalancer
  selector:
    component: flask-portfolio
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000